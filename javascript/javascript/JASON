<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body> 
    <!--<input type="button" id="myButton" value ="click">-->
    <!--<div id="container">-->
    <!--<input type="text" id="fname" oninput="reverseText()">-->
    <!--<p id="fname">This is a paragraph</p>-->
    <!--</div>-->
    <!-------------------------------------------------------------------------------------------------------------->
    <!----------------------------------------------EMAIL VALIDATION------------------------------------------------>
    <!--<input type = "email"id="email">
    <button onclick = "validateEmail()">Submit</button>-->
    <!-------------------------------------------------------------------------------------------------------------->
    <!----------------------------------------------NUMBER VALIDATION ---------------------------------------------->
    <!--<input type="text" id="age">
    <button onclick="validateNumber()">Submit</button>
    -->
    <!-------------------------------------------------------------------------------------------------------------->
    <!------------------------------------------PASSWORD STRENGTH VALIDATION --------------------------------------->
    <!--
    <input type="password" id="password">
    <button onclick="validatePassword()">Submit</button>
    -->
    <!------------------------------------------------------------------------------------------------------------->
    <!---------------------------------------REAL-TIME VALIDATION WITH ONINPUT EVENT------------------------------->
    <!-------------------------------<input type="text" id="username" oninput="checkLength()">--------------------->
    <!------------------------------------------------------------------------------------------------------------->

    <!--Using CSS-->
    <!--<button onclick="document.body.style.backgroundColor='lightblue'">Change Background</button>-->
    <!--Ondocus-->
    <!--<input type="text" onfocus="this.value='You clicked me!'"/>-->
    <!--<h1 onclick="this.style.color='red'">Click on this text !</h1>-->
    <!--<form name="myForm"onsubmit="return validateForm()">
        Name: <input type="text" name="fname">
        <input type="submit" value="Submit">
    </form>-->
    <script>
   //------------------------------------------------------------------------------------------------------------------------
   //--------------------------------------JAVASCRIPT JSON (JAVASCRIPT OBJECT NOTATION)---------------------------------------
   //------------------------------------------------------------------------------------------------------------------------
   /*let num1='{"name":"John","age":30,"college":"MSRIT","income":false,"married":null}';
   let num2 = JSON.parse(num1);

   console.log(num2);
   console.log(num1);
   */
   //------------------------------------------------------------------------------------------------------------------------
   /*let person ={name:"John",age:30,college:"MSRIT",income:false,married:null};
   let num2= JSON.stringify(person);
   console.log(num2);*/
   //------------------------------------------------------------------------------------------------------------------------
   //----------------------------------------------------declaring json array------------------------------------------------
   /*
   let jsonData=
        {"name":"john","age":30,"skills":["python","html","css"]};

        //UPDATING JSON
        jsonData.income=25000;
        jsonData.name="sree";
        console.log(jsonData);*/

    //-----------------------------------------------------------------------------------------------------------------------
    //--------------------------------------------------Accessing JSON Data--------------------------------------------------
    /*let obj=
        {"name":"john","age":30,"skills":["python","html","css"]};

        console.log(obj.name);//Output:John
        console.log(obj["age"]);//Output: 30*/                              
       /*let x=10;
       const y=15;
       console.log(x);
       console.log(y);
       x=20;
       console.log(x);
       //y=30;// y is a constant [ERROR WILL OCCUR]
       //console.log(y);// Error will come because of we can't change the value for the constant..
       */
       //------------------------------------------------------------------------------------------------------------------------
       /*
       let add=(a,b) => a+b;
       console.log(add(10,15));//OUTPUT is 25...
       
       let sub=(a,b) => a-b;
       console.log(sub(15,10));//OUTPUT is 5...
    
       let mul=(a,b) => a*b;
       console.log(mul(5,10));//OUTPUT is 50...
       
       let div=(a,b) => a/b;
       console.log(div(10,5));//OUTPUT is 2...
       */
       //-------------------------------------------------------------------------------------------------------------------------
       /*
       let numbers=[10,20,30]
       for (let num of numbers)
       {
        console.log(num);//OUTPUT IS [10 20 30]
       }
       */
       //------------------------------------------------------------------------------------------------------------------------
       /*
       let str="hanush";
       for(let s of str)
       {
        console.log(s);//OUTPUT IS h a n u s h it will print each letter un each line .
       }
       */
       //------------------------------------------------------------------------------------------------------------------------
       /*
       //Back-Ticks Synatx: Template Strings use back-ticks(``)insted of quotes...
       let text1=`Hello World`;
       console.log(text1);// OUTPUT IS Hello World...
       //Quotes Inside Strings Template Strings allow both single and double quotes inside a string...
       let text2=`He's often called "Johnny"`;
       console.log(text2);// OUTPUT IS He's often called "Johnny"...
       //Multiline Strings Template String allow double quotes inside a string ....
       let text= `The quick,
        brown fox
        jumps Over
        the lazy dog`;
        console.log(text);
        //For this the output will come as it is as input ...
        // let num =5;
        console.log(`The value of num is ${num}`);//The value of num is 5
        */
       /*
        //String Interpolation
        //syntax:${...}
        //Variable Substituation : Template String allow variable in string...
        let firstName = "John";
        let text = `Welcome ${firstName}`;
        //Expression Substitution: Template String allow expressions inside strings.
        let  radius = 5;
        let area = `The area of a circle with radius $ {radius} is ${(Math.PI * radius ** 2).toFixed(2)}`;
        console.log(area);
        */
       //------------------------------------------------------------------------------------------------------------------------
        //Map Objects
        //-> A Map holds key-value pairs where keys can be any type.
        //-> Maps remember the order of insertion.
        //-> Maps provide useful methods for working with data.
    
        //Example.
        /*
        const fruits =new Map([
            ["apple", 500],
            ["bananna", 300],
            ["orange", 200]
        ])
       //console.log(fruits.get("apple"));//Output is 500...
        console.log(fruits.get("apple"));//Output is 500...
        console.log(fruits.size);//Output is 3...
        console.log(fruits.delete("bananas"));//Output is true...
        console.log(fruits.size);//Output is 2...
        console.log(fruits.set("grapes",700));//Output is true...
        console.log(fruits.has("applesss"));//Output is false...
        console.log(fruits.clear());//Output is undefined...
        console.log(fruits.size);//Output is 0...
        */
        //-----------------------------------------------------------------------------------------------------------------------
        //Set Objects 
        // A Set is a collection of unique values.
        //It does not allow duplicate values.
        //Sets are usefull for storing distinct elements.
        //Example.
        /*
        const letters = new Set();
        letters.add("a");
        letters.add("b");
        letters.add("c");
        console.log(letters.has("b"));//true
        // add(value)-Adds a new value 
        //delete(value)-Remove a value 
        //has(value) - Checks if a value exists.
        //size - Retuen the number of elements.
        //clear(value) - removes all elements.
        these are the set methods.
        */
